// Package memory is the canonical memory contract: the interfaces and
// data types shared by llm-agent and llm-agent-memory. It carries no
// engines, no concrete Manager, and no scoring — only the types,
// sentinel errors, and metadata helpers that both consumers agree on.
package memory
